# NxtMarket — Project Overview (Plain HTML Templates)

**NxtMarket** is a moderate-level capstone where students build a mini multi‑vendor marketplace using Node.js. It demonstrates: Node fundamentals, filesystem operations, core HTTP, plain HTML templates, Express MVC, Mongo/Mongoose, SQL/Sequelize, JWT authentication, WebSockets, and optional Redis caching.
